# Remi 1.0 — 작업 규칙

식품 배합 포뮬레이터. **무엇을 얼마나 넣을지를 최소한의 실험으로 찾는다.**

    app/          Streamlit. 사용자 화면(단계형)과 전문가 화면(5탭)
    engine/       혼합물 모형 · v2 어댑터 · 자체검사
    ontology_v2/  L·A·M·C·S·R 레이어 + 정본 로더(tests/loader_reference.py)
    tools/        온톨로지를 고치는 일회성 도구. 전부 --check 를 갖는다
    docs/         전략 문서. 착수 전에 읽는다

---

## 일하는 방식 — 지키기로 한 것

2026-09-07, 사용자 피드백: *"특정 제형·단어·제품에 너무 집착해 넓은 시야로
일을 못 하는 것 같다."* 맞는 지적이었다. 되짚어 보니 패턴이 있었다.

| 한 일 | 놓친 것 |
|---|---|
| `requires` 를 216개 항목에 붙이자고 제안 | 도달 가능성으로 이미 계산되고 있었다 |
| 판정 항목을 제형의존/제품의존으로 나눔 | **재료의존**이 빠져 있었다 |
| "A2 를 합치면 비용 1/3" 이라 주장 | 실제로 재니 세션당 $0.006 차이였다 |
| 엣지 결손 8건을 하나씩 판정 요청 | 규칙을 못 찾고 사례를 나열했다 |
| 렉시콘 정의를 보고 아이스크림 질문 | 그 정의 자체가 틀린 것이었다 |

공통점은 **구조적 답이 있는데 사례를 나열한다**는 것이다. 그래서 아래를 지킨다.

**사례를 나열하기 전에 규칙을 찾는다.** 같은 종류의 판정을 세 번 넘게 요청하게
되면 규칙을 놓친 것이다. 멈추고 규칙을 찾는다.

**온톨로지 문자열을 사실로 받지 않는다.** 그것은 물리일 수도 있고 저작 이력일
수도 있다. 실제로 셋 다 이력이었다 —

    아이스크림에 짠맛 카드가 없음     -> 물리가 아니라 안 쓴 것
    현탁액 meta 의 "default product"  -> 제형 파일에 제품이 들어앉은 것
    펴발림성 정의의 "scooping"        -> v1 scoopability 를 잘못 흡수한 것

의심스러우면 **다른 신호와 교차**시킨다. 카드 유무 · 엣지 스코프 · 도달 가능성 ·
같은 부류의 다른 프로파일. 하나만 보면 이력에 속는다.

**기본값을 정해 제안하고 거부권을 드린다.** 판정을 먼저 요청하지 않는다. 다만
**배합 지식이 필요한 것**(크리미함이 지방을 요구하는가, 백탁과 탁함이 다른가)은
추측으로 고치지 않는다 — 틀리면 조용히 오염된다. 그런 것은 **묶어서 한 번에**
여쭙고 나머지는 진행한다.

**수치를 먼저 재고 주장한다.** "비용 1/3", "10건쯤" 같은 것을 재지 않고 말하지
않는다. 이 저장소는 대부분 계산으로 확인된다.

---

## 레이어 이름 — 용어집

2026-09-10 에 글자를 낱말로 바꿨고 09-11 에 두 번 더 고쳤다. **파일명과 ID 는
그대로**라 다리가 필요하다.

| 이름 | 파일 | 무엇을 담나 | 담긴 ID |
|---|---|---|---|
| `LEXICON` | `layerL_lexicon.yaml` | 감각 용어 216개. 맥락 없는 정의 | `L.*` |
| `PARAMETER` | `layerA_parameters.yaml` | 물성 39개. 이름·단위·측정법 | `P.*` |
| `INGREDIENT` | `layerC2_*.yaml` | 재료 데이터베이스. 재료·기능군 -> 물성 엣지 | `ING.*` `FT.*` |
| `RELATION` | `layerR_seed.yaml` | 물성 -> 감각(R-1), 감각 <-> 감각(R-2) 등 | `PO.*` `IN.*` |
| `STRUCTURE` | `layerS2_profiles.yaml` | 제형 4개. 정의 파라미터·범위·필러 | `SC.*` |
| `AXIS_CARD` | `layerM_cards_<제형>.yaml` | 제형별 축 카드. 이 축을 여기서 어떻게 읽고 점수 매기나. **제품 사이에 공유** | (카드) |
| `PRODUCT_CARD` | `projects/<제품>/product_card.yaml` | **제품당 한 장.** 제형 축 카드에서 벗어나는 것만 — 정체성 축, tier·goal 바꾼 것, 실측 열, 뺀 축 | (온톨로지 밖) |
| `PROCESS` | *(아직 없음)* | 공정. 파라미터 위의 두 번째 액추에이터 | |

**카드는 두 층이다 (09-11 결정).** 재 보니 옛 제품 카드 16장 중 8장이 제형
카드의 복사본이었다. 제품이 실제로 다르게 가진 정보는 새 축 셋과 값 몇 줄뿐
이다. 제품마다 통째로 복사하면 제품 30개에 단맛 앵커가 30벌 생기고 갈라진다.
그래서 `AXIS_CARD`(공유) + `PRODUCT_CARD`(차이) — 재료 데이터베이스가
기능군(공통) + 재료 override(특정)인 것과 같은 2-tier 다. 합성 규칙은
`v2adapter._compose_product_cards`: 제형 카드 전부 → `drop` 뺌 → `axes` 로
덮거나 더함 → 보편 축.

**왜 바꿨나.** 일곱 중 넷은 글자가 이미 뜻이었다(L·R·S·M). 나머지 셋이 어긋나
있었다 — `A` 는 담긴 ID 가 `P.*` 인데 글자가 A 라 파일을 열지 않으면 알 수
없었고, `C` 는 무엇의 약자인지 어디에도 없었고, `O` 는 만들지도 않은 층인데
글자만 박혀 있었다. 실제로 내가 온톨로지 지도에서 `Layer A` 를 "재료와 관능
사이의 배선" 이라고 잘못 적었다. 사전인데.

**09-11 에 다시 고친 이유.** 첫 개명이 `MEASUREMENT` 와 `EFFECT` 를 박았는데
둘 다 틀렸다. 카드에 실측값은 한 줄도 없다 — 앵커 문장·평가 주의·합격선·tier,
곧 **한 축을 이 맥락에서 어떻게 읽을지**의 정의다. C 층은 재료 데이터베이스인데
이름이 그 안의 엣지를 가리키고 있어 `INGREDIENT` 로. 같은 날 오후 두 층으로
가르면서 제형 층은 `AXIS_CARD` 가 되고 `PRODUCT_CARD` 는 제품 한 장에 붙었다.
`engine/formulator/cypher.py` 의 `"EFFECT"` 는 Neo4j 관계 타입이라 레이어 이름이
아니다 — 그대로 둔다.

**`Layer B` 를 만나면.** v1 이 지금의 `LEXICON` 을 그렇게 불렀다. 렉시콘 안의
v1.1 감사 이력 4건은 그때 실제로 그 이름이었으므로 남겼다.

**v1 로더는 글자가 다른 뜻이다.** `engine/formulator/ontology.py` 계열 —

    v1 A = structure classes    v2 STRUCTURE
    v1 B = sensory attributes   v2 LEXICON
    v1 C = tags + ingredients   v2 INGREDIENT (같다)

그래서 `tools/rename_layers.py` 가 이 파일들을 건너뛰고 손으로 고쳤다.

**세 질문을 이 이름으로 다시 쓰면** — 존재는 `INGREDIENT`+`RELATION` 도달 가능성이,
재는 법은 `AXIS_CARD` 가, 최적화 대상인지는 tier(제형 기본을 `PRODUCT_CARD` 가
덮는다)가 정한다.

---

## 온톨로지를 고칠 때

**정본 로더(`ontology_v2/tests/loader_reference.py`)는 건드리지 않는다.**
확장이 필요하면 `V2Ontology` 에서 감싼다 — `_install_universal_axes()` 가 그 예다.
앱이 `onto._ref.load_cards` 를 직접 부르는 곳이 16군데라 거기서 감싸야 전부가
같은 것을 본다.

**세 질문을 섞지 않는다.**

    이 감각이 존재하나       -> 도달 가능성.  경로가 있으면 존재한다
    여기서 어떻게 재나       -> M 카드.      제형에 걸린다
    여기서 최적화 대상인가   -> tier.        제형·제품에 걸린다

첫 줄의 답을 카드 유무로 하지 않는다. 그것이 짠맛이 아이스크림에서 사라졌던 이유다.

**축이 존재하는 이유는 다섯이다.**

    보편        전제 없음                     짠맛
    제형의존    제형이 그 상을 줘야 한다      얼음 씹힘 <- 얼음
    재료의존    그 재료가 팔레트에 있어야     커피 향 <- 커피
    공정의존    공정이 정한다                 꺼끌거림 <- 얼마나 갈았나
    엣지결손    존재하는데 엣지가 없다        고쳐야 할 것

"제품의존" 은 없다. 제품이 정하는 것은 존재 여부가 아니라 **무엇을 앞세울지**
(정체성 축 2~3개)이고, 그건 tier 의 일이다.

**공정의존은 2026-09-10 판정 대장에서 나왔다.** 넷으로 두고 있었는데 사용자가
건더기 크기 칸에 적어 주셨고, 재 보니 스펙 5.9 가 "액추에이터 없는 R-1" 이라
플래그를 달던 파라미터 셋을 정확히 설명한다 — `P.particle_size_d90`(꺼끌거림)
`P.flavor_release_dynamics`(뒷맛 길이) `P.serving_temperature`(차가움). 셋 다
재료 엣지 0건이고, 그것이 당연하다. 결함으로 보였던 것은 이름이 없어서였다.

이 자리에서 배운 것: **범주가 모자라면 결함처럼 보인다.** 플래그가 오래
남아 있으면 고칠 것을 못 고친 것이 아니라 **부를 이름이 없는 것**일 수 있다.

**YAML 을 고칠 때 주의.** 이 파일들에는 앵커(`&id001`)와 주석이 있어
`safe_load -> safe_dump` 를 거치면 둘 다 사라진다. 텍스트로 끼워 넣고,
앵커를 정의하는 항목을 지우면 남은 첫 참조를 정의로 승격시킨다.
`counts:` 는 한 줄이 아니라 중첩 블록이라 자식 줄까지 갈아야 한다.

**도구는 `--check` 를 갖는다.** 넣기 전에 무엇이 빠졌는지 볼 수 있어야 하고,
두 번 돌려도 덮어쓰지 않아야 한다.

---

## 고치면 반드시 돌리는 것

    python engine/selfcheck_v2.py                 # 온톨로지 44건
    python engine/selfcheck_model.py              # 엔진 18건
    python ontology_v2/tests/loader_reference.py  # 5 프로파일
    python tools/audit_axis_scope.py              # 축 의존성 감사

앱까지 보려면 사용자 흐름 7제품을 돌린다. 검사가 빨간불이면 그것이 실제 결함일
때만 그대로 두고, 왜 뒀는지 커밋 메시지에 적는다.

---

## 지금 서 있는 자리

**2026-09-11 부터 세션을 기능별로 가른다** — [`docs/session_split.md`](docs/session_split.md).
S1 ONTOLOGY · S2 INGREDIENT · S3 MODEL · S4 LANGUAGE · S5 APP. 한 파일은 한 세션만 고친다.
새 세션은 그 문서의 6절 프롬프트로 시작한다.

착수 순서는 [`docs/상호작용_알고리즘.md`](docs/상호작용_알고리즘.md) 9 절.

| | | |
|---|---|---|
| 1 | 질문 선택 정책 (`B1 SCOPE`) | 완료 |
| 1.5 | 현탁액에서 발효 핫소스 분리 | 완료 |
| 1.7 | 공정 기록 1단계 (`C0 PROCESS`) | 완료 |
| 1.8 | 축 의존성 감사 · 엣지 결손 채우기 | 엣지 완료, 대장 남음 |
| 2 | 제품 사전 초안 30 | |
| 3 | 재료 검색 · 잠정 등록 · 빈 곳 제안 | |
| 4 | 서술형 파싱 | API 필요 |
| 5 | PROCESS (공정 2단계) | |

**승인 대기 중인 것** — `docs/척도정의_전략.md` 의 결론(1 단계 ≈ 1 JND,
`RANGE_TO_SD` 4.0→2.0 등)은 사용자 승인 전까지 구현하지 않는다.

**판정 대장** — `data/axis_review.xlsx` 22행 완료(2026-09-10). 감사 도구가
이제 읽는다: 판정된 항목은 ③에서 빠지고 ④에 판정이 붙는다. `--xlsx` 로
재생성해도 판정·메모는 보존되고 도달 제형 숫자만 갱신된다.

**열려 있는 것** — DoE 실험 횟수. 전제는 "적을수록 좋다, 첫 배합은 하나여도
좋다". 실측에 반복(rep)이 한 건도 없어 σ 를 잰 적이 없다는 것이 핵심 제약이다.
