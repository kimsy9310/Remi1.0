numpy>=1.24
openpyxl>=3.1
pandas>=2.0
pyyaml>=6.0
# scipy 는 선택이 아니다. 없으면 propose() 가 조용히 경사하강으로 물러난다.
# mixture.py 가 잰 오차(±3 관능 척도): SLSQP 0.019 ↔ 경사하강 0.950 (트러플
# 마요). 이 저장소의 현재 7 프로파일에서는 최대 0.22(아이스크림 단맛)였다.
# 멈추지 않고 틀리는 쪽이라 명시한다.
scipy>=1.10
streamlit>=1.30
